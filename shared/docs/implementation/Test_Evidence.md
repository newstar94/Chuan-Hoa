# Test Evidence

| Test ID | Trạng thái | Lệnh/môi trường | Kết quả |
| --- | --- | --- | --- |
| BASE-PROJECT-001 | PASS | Kiểm tra source/target sau copy có loại trừ dependency/build cache | 154 baseline files được sao chép; project mới độc lập |
| BASE-INVENTORY-001 | PASS | `python tools/baseline/generate_inventory.py` | Sinh inventory, hashes và evidence JSON |
| RIBBON-BASE-001 | PASS | Đọc `customUI/customUI14.xml` từ `.dotm` | 1 tab, 7 groups, 36 buttons, 4 menus, 2 dropdowns, 3 checkboxes, 45 controls |
| VBA-BASE-001 | PASS | Phân tích 68 file trong `shared/vba_extracted` | 68 modules, 363 public entry points, ledger đầy đủ |
| RULE-BASE-001 | PASS_WITH_CORRECTION | Parser RulesPart1–5, registry, JSON và backend checker | 96/94/75/19/52/14; correction được ghi ADR-004 |
| ENV-BASE-001 | PASS_UPDATED | `vswhere`, reference assemblies và VSTO target discovery | Build Tools 17.14.37614.0, net48 Targeting Pack và VSTO Office targets đã được cài; Word 16.0.20326.20112 x64 |
| RIBBON-CONTRACT-001 | PASS_SOURCE_2026_09_04 | `python tools/vsto/generate_vsto_ribbon.py`; `python tools/vsto/validate_vsto_source.py` | Target contract có 42 interactive control: 34 button, 3 menu, 2 dropdown và 3 checkbox; QR đã retired, nhóm Hiển thị được giữ; thêm `Sửa lỗi đang chọn`; bỏ `Đọc dữ liệu`, Save-as-DOCX và i/y; giữ riêng hai kiểu đặt dấu; Từ điển cá nhân dùng callback icon custom |
| SERVER-BUILD-001 | PASS | `.tools/dotnet/dotnet.exe test ChuanHoa.slnx -c Release --no-restore` | 12 project build; 0 warning, 0 error |
| SERVER-TEST-001 | PASS | `dotnet test ChuanHoa.slnx --no-restore` ngày 2026-09-04 | Domain 14, contract 4, API 11, application 6, canonical rules 10, client core 201; tổng 246/246 PASS (integration DB ghi riêng) |
| RULE-CANONICAL-001 | PASS | `ChuanHoa.Rules.Tests` | 10/10 strict parser, unknown field/enum, publication legal/implementation/fixture và corpus gate tests |
| RULE-CANONICAL-BASELINE-001 | PASS_DRAFT_ONLY | `python tools/rules/generate_canonical_baseline.py` | 96 draft rule: 75 logic path, 19 hardwired NotChecked, 2 unrouted; publishable=false |
| DB-MIGRATION-V001-001 | PASS | `tools/database/verify_v001.ps1` | PostgreSQL 17.11: up/down, 22 table, overlap, immutable event và replay/idempotency constraints PASS |
| DB-PERSISTENCE-001 | PASS | `tools/database/verify_persistence.ps1` | 4/4 Npgsql integration test: acquire/conflict/replay/retry và outbox commit/rollback |
| API-BOUNDARY-001 | PASS | `ChuanHoa.Api.Tests` và OpenAPI runtime probe | Correlation header/body khớp, error schema `chuanhoa.error.v1`, mutation key validation, OpenAPI 3.1.1 không chứa hostname |
| TECHNICAL-SCAN-001 | PASS_FOUNDATION_FAIL_CLOSED | `ChuanHoa.Application.Tests` và API TestServer | 6/6 scanner + authorization boundary PASS; exact anchor cho khoảng trắng/dấu câu; font/section anchor; schema/format/spelling fail closed; API không có IdP trả 401 |
| LOCAL-ACCESS-001 | PASS_DEVELOPMENT_E2E | `Start-ChuanHoaDevelopment.ps1`; Development access smoke | Signed RS256 lease/rule verify; device/client/feature binding; lease 7 ngày; Development key tách Release; `FORMAT_SCAN`, `SPELLING_SCAN`, `DOCUMENT_TOOLS` |
| WORD-HANG-REGRESSION-001 | PASS_WORD16_X64_DOC_DOCX | Event Log/WER; `validate_vsto_source.py`; VSTO Development build; access/local scan/local command/1-Click/annotation smoke | Xác nhận AppHangTransient và crash `wwlib.dll`/`0xc0000005` trên bản cũ; bản mới không refresh license đồng bộ, không dùng `FinalReleaseComObject`, không đọc Selection trong callback enable. Vòng lặp xóa trang thừa trên đoạn cuối bắt buộc của Word đã được thay bằng bộ dọn đuôi có giới hạn và một lần xóa; DOC/DOCX local-command và 1-Click smoke PASS. |
| COMMAND-SCOPED-ANALYSIS-001 | PASS_SOURCE_BUILD_COM_LOAD_WORD16_X64 | `generate_ribbon_contract.py`; `generate_vsto_ribbon.py`; `validate_vsto_source.py`; full solution; DOC/DOCX synthetic smoke; Word COM load | Không còn nút hoặc điều kiện tiên quyết `Đọc dữ liệu`. Chỉ lệnh do người dùng bấm mới gọi runtime phân tích; 1-Click quét cả trước và sau mutation để chỉ comment lỗi còn lại. WindowActivate/Startup/Save không tự đọc nội dung; WindowActivate của recovery clone ẩn không thay thế tài liệu người dùng. Cache fail closed theo saved state, fingerprint, revision, lane và RulePackId. 246/246 test PASS; 1-Click DOC/DOCX regression PASS; VSTO 1.0.0.82 đã cài và COM load trên Word 16 x64. |
| ONE-CLICK-SAVE-INVALIDATION-001 | PASS_SOURCE_BUILD_WORD16_X64_DOC_DOCX_REAL_COPY | `ChuanHoa.OneClickSmoke.exe`; bản sao `1.5. QĐ phê duyệt kế hoạch - THCS Ngọc Hà.docx`; VSTO Development build | Tái hiện lỗi: sau scan/sửa lỗi, tài liệu chưa lưu; `DocumentBeforeSave` xóa analysis giữa Prepare và Execute làm phát sinh `ArgumentNullException(snapshot)`. Runtime mới lưu trước, lấy lại context đúng ActiveDocument, bắt buộc dựng full snapshot sau sự kiện lưu và kiểm tra đồng nhất fingerprint/revision/lane/rule-pack trước khi dùng. Regression DOC/DOCX kích hoạt đúng một save-invalidation rồi 1-Click PASS; bản sao tài liệu thật PASS với 19 đoạn định dạng, 9 lỗi chính tả sửa, 1 line xử lý; file gốc không bị sửa. |
| HEAVY-DOCUMENT-READ-001 | PARTIAL_PASS_RULE_SOURCES | Full memory dump WINWORD 1.0.0.31; snapshot smoke 2026-09-04 trên `Hướng dẫn 05.docx` và `Nghị định 30.doc` | Bằng chứng lịch sử xác định `WdInformation` là điểm nghẽn. Source đã bỏ `Application.DoEvents`; snapshot hai tài liệu quy tắc thật hoàn tất và xác nhận không sửa byte/timestamp nguồn. Lane 10/50/100 trang, progress/cancel và tài liệu nhiều bảng của người dùng vẫn phải chạy lại trước khi ghi PASS đầy đủ. |
| RULE-ONLY-PRODUCT-001 | PASS_SOURCE_BUILD_TEST | `python tools/validation/validate_rule_only_product.py`; full solution build/test | Không còn engine/IPC/model/training trong source/runtime/test; installer dùng allowlist và có payload deny gate. Bản cài mới chưa đóng gói vì Word đang mở. |
| PERSONAL-DICTIONARY-001 | PASS_UNIT_SOURCE_BUILD_INSTALL | 166 test `ChuanHoa.Client.Core.Tests`; VSTO Development rebuild; installer 1.0.0.78 | NFC/offset, duplicate, giới hạn input, typed I/O failure, atomic persistence, phục hồi `.last-good`, current/all ignore API và production scanner document scope PASS. Icon PNG 16/32 có alpha, nhúng trong DLL cài đặt và callback fail-safe; dialog/icon còn cần Word visual smoke DPI 100–200%. |
| TABLE-RANGE-MAPPING-001 | PASS_WORD16_X64_HEAVY_DOCX | `ChuanHoa.LocalScanSmoke.exe "4. QĐ phê duyệt KHLCNT.docx"`; `ChuanHoa.OneClickSmoke.exe "4. QĐ phê duyệt KHLCNT.docx"`; snapshot smoke trên 4 tài liệu | Tái hiện chính xác lỗi `0x800A1200 Value out of range`: finding P1380 bị ánh xạ sai tới start 17.174 vì `Range.Text` biểu diễn ký hiệu cuối ô khác hệ tọa độ Word. Sau sửa, mọi paragraph dùng Start/End có thẩm quyền từ COM Range; format annotation 11.875 ms, spelling annotation 5.826 ms và 1-Click sửa 49 đoạn/35 lỗi chính tả, không còn range vượt giới hạn. Snapshot regression kiểm tra mọi main-story anchor nằm trong `Document.Content`. |
| LOCAL-SCAN-001 | PASS_UNIT_AND_WORD16_X64_DOC_DOCX | `ChuanHoa.Client.Core.Tests`; `ChuanHoa.LocalScanSmoke.exe` | 178/178 toàn client core; local scan có canonical format/spelling/capitalization/abbreviation/Telex/từ vựng tiếng Việt/Line Shape, exact anchors và Word thật DOC/DOCX; không có i/y; giữ hai lệnh đổi cách đặt dấu riêng |
| DOCUMENT-TYPE-AUTO-001 | PASS_UNIT_AND_WORD16_X64_DOC_DOCX | `DocumentRoleDetectorTests`; `ChuanHoa.LocalScanSmoke.exe`; `ChuanHoa.OneClickSmoke.exe` | Nhận diện 26 dòng tên loại NĐ30 và Công văn qua `V/v`/`Về việc`; hỗ trợ footnote nhỏ; nội dung thắng lựa chọn cũ; context tự cập nhật thành Quyết định trong format scan và 1-Click Word thật trên DOC/DOCX |
| LOCAL-RULE-PORT-001 | PASS_SOURCE_AND_SYNTHETIC_CORPUS | `CanonicalRouteScannerTests`; `CanonicalRuleScanner.RegisteredRuleCodes` | Registry unique gồm 72/72 route baseline sản phẩm còn hiệu lực + 4 detector Line Shape + 1 route từ vựng; route cảnh báo section ngang `ND30-PL1-M1-K2` đã gỡ và có regression không sinh finding; positive corpus phát finding cho từng code còn lại. Chưa thay thế legal sign-off/golden corpus thật |
| WORD-LINE-SHAPE-001 | PASS_UNIT_AND_WORD16_X64_DOC_DOCX_PDF | Audit ba tài liệu chuẩn; `CanonicalRouteScannerTests`; `ChuanHoa.LocalScanSmoke.exe`; `ChuanHoa.OneClickQa.exe`; VSTO Development build; QA bản sao quyết định thật | Chỉ chấp nhận `msoLine` nét liền, không mũi tên, đúng story/section/page/vị trí/chiều dài/cân giữa; Line Shape dưới Tiêu ngữ phải dài bằng phần chữ với dung sai 95–105%, sai số tâm ngang tối đa `max(8 pt, 4% chiều rộng chữ)` và nằm ngay dưới chữ. Tọa độ Word đã bao gồm padding ô nên runtime không cộng padding lần hai. 1-Click chủ động đặt lại mọi line bắt buộc, kể cả Shape được snapshot cũ coi là tồn tại; line cũ lệch/ẩn được tự chữa, underline và paragraph border không thay thế. DOC/DOCX COM smoke PASS; PDF 3 trang xác nhận tâm line cơ quan `188,08 pt`, Tiêu ngữ `443,22 pt`, và post-scan không còn finding Line Shape; build 0 warning/error. |
| ONE-CLICK-REAL-LAYOUT-002 | PASS_WORD16_X64_COPY_ONLY | `ChuanHoa.OneClickQa.exe` trên bản sao tài liệu quyết định THCS Ngọc Hà; PDF 3 trang | Bản 1.0.0.55 áp dụng `12–13–13`, Quốc hiệu một dòng; thay/đặt lại Line Shape dưới cơ quan, Tiêu ngữ và trích yếu. PDF xác nhận line Tiêu ngữ dài đúng phần chữ, thẳng và cân tâm. `Nghị định 214...` thành `Nghị định số 214...`; post-rescan có 2 format, 0 spelling, đúng 2 comment section ngang; không còn finding Line Shape hoặc comment thiếu `số`; file nguồn không đổi. |
| ND30-HEADER-FONT-TIER-001 | PASS_UNIT_AND_WORD16_X64_REAL_COPY | `Nd30HeaderFontSizeTierResolverTests`; scanner/1-Click/preset profile; QA bản sao THCS Ngọc Hà | Chỉ có hai bộ đồng nhất: Quốc hiệu–Tiêu ngữ–địa danh/ngày tháng `12–13–13` hoặc `13–14–14`. Khi Quốc hiệu hợp lệ thì tier của Quốc hiệu quyết định; khi sai/mixed thì suy ra theo đồng thuận Tiêu ngữ và ngày, nếu vẫn không rõ mặc định `12–13–13` để tránh xuống dòng. Tài liệu thật được sửa từ `12–14–14` về `12–13–13`. |
| APPENDIX-MODEL-001 | PASS_UNIT_SOURCE_AUDIT_WORD16_X64_REAL_COPY | `DocumentRoleDetectorTests`; `CanonicalRouteScannerTests`; Phụ lục NĐ30 và PDF audit; QA bản sao THCS Ngọc Hà | Hỗ trợ Mẫu 2.1 và 2.2: số Phụ lục dùng Ả Rập hoặc La Mã; nhiều phụ lục vẫn phải có số; nhãn 14 pt đậm căn giữa; một hoặc nhiều dòng tên Phụ lục viết hoa, 13–14 pt đậm căn giữa; dòng `Kèm theo...` 13–14 pt nghiêng căn giữa; Mẫu 2.2 nhận thông tin ký số phía trên bên phải Times New Roman 10 pt; mỗi phần Phụ lục phải bắt đầu lại số trang. 1-Click định dạng toàn bộ các dòng tiêu đề được nhận diện và dòng viện dẫn. |
| APPENDIX-PAGINATION-001 | PASS_WORD16_X64_REAL_COPY_PDF | `WordAppendixPaginationNormalizer`; `ChuanHoa.OneClickQa.exe`; PDF render trang Phụ lục | Tái hiện bản QA: tiêu đề Phụ lục ở section 2, bảng ở section 3; hai section cùng landscape/hình học nhưng section 3 bắt đầu trang mới. 1-Click đặt KeepWithNext cho cụm tiêu đề và đổi duy nhất ngắt dư sang continuous khi hình học hai section giống nhau. Kết quả: 3 -> 2 trang; Phụ lục vẫn ngang; bảng cùng trang tiêu đề; post-format 0, post-spelling 0, comment 0. Section khác hướng không bị gộp. |
| LOCAL-COMMAND-WORD-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalCommandSmoke.exe` | DOC/DOCX PASS: Unicode TCVN3/VNI core, 19 style, bộ cỡ chữ role-aware, hai kiểu đặt dấu, xóa trang thừa và toàn bộ lệnh còn lại; QR đã retired; lệnh cục bộ không tự save và không tạo recovery clone |
| TRAILING-BLANK-PAGE-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalCommandSmoke.exe`; `ChuanHoa.OneClickSmoke.exe`; `validate_vsto_source.py` | Tái hiện nguyên nhân: Word không cho xóa paragraph sentinel cuối sau bảng, `Paragraphs.Count` không đổi nên vòng lặp cũ không thoát. Runtime mới bỏ qua sentinel, không xóa table-cell marker/ngắt trang/ngắt section, quét tối đa 4.096 paragraph và xóa cả đuôi trong một thao tác. Chạy lại lần hai trên tài liệu kết thúc bằng bảng trả về dưới 5 giây, giữ nguyên bảng; DOC/DOCX PASS. |
| CHARACTER-SPACING-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalCommandSmoke.exe`; đối chiếu `TextFormatter.bas.bas` | Co/giãn chỉ tác động đúng span đang bôi chọn theo bước 0,1 pt; phần ngoài selection giữ 0 pt; reset về 0 pt; selection rỗng fail rõ và không sửa/undo cả paragraph; mixed spacing lấy 0 pt làm mốc |
| BACKUP-POLICY-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalCommandSmoke.exe`; `ChuanHoa.OneClickSmoke.exe` | Chỉ 1-Click và hai lệnh đổi cách đặt dấu trả về/tạo backup; Sửa lỗi đang chọn và các lệnh cục bộ trả rỗng; backup giữ đúng `.doc`/`.docx` dưới `%TEMP%\ChuanHoa\Backups`; dọn file sở hữu quá 7 ngày, giữ tối đa 20 bản và không xóa file không thuộc add-in |
| ACTIVE-DOCUMENT-CHAIN-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalCommandSmoke.exe` | DOC/DOCX PASS chuỗi FormatPage -> KeepWithNext mà không clone/save cưỡng bức; source `FullName` vẫn là ActiveDocument và capability trên source là `READY`; hai lệnh đổi dấu có clone khôi phục nhưng source vẫn được kích hoạt lại |
| DEVELOPMENT-ADMIN-001 | PASS_LOCAL_FOUNDATION | `ChuanHoa.Api.Tests`; browser loopback | State store persistence test; trang quản trị responsive; endpoint chỉ Development + loopback; production auth/RBAC không được coi là PASS |
| VSTO-SOURCE-001 | PASS_SOURCE_ONLY | `python tools/vsto/generate_vsto_ribbon.py`; `python tools/vsto/validate_vsto_source.py` | 1 tab, 42 control, 58 callback; 34/34 button có handler; net48, Office 14.0, không task pane/static document; không còn `Đọc dữ liệu`, Save-as-DOCX, QR hoặc i/y; có hai kiểu đặt dấu và Từ điển cá nhân |
| CLIENT-SAFETY-KERNEL-001 | PASS_FOUNDATION_INTEGRATED_FAIL_CLOSED | `dotnet test tests/ChuanHoa.Client.Core.Tests -c Release` | 27/27 test; `.doc`/`.docx` được phép, định dạng khác fail closed; RS256/replay/Word adapter vẫn khóa mutation khi thiếu production key/grant/golden evidence |
| ANNOTATION-PLANNER-001 | PASS_UNIT | `dotnet test ChuanHoa.slnx --no-restore` | 15 annotation test + 1 contract round-trip: exact span, repeated text, stale offset, paragraph/section/document, table cell, header, protected span, overlap, idempotency/conflict, ambiguity, fingerprint/revision và ownership/color preservation |
| ANNOTATION-VSTO-ADAPTER-001 | PASS_WORD16_X64_LOCAL_SMOKE | Hidden Word COM smoke | Apply/rerun/clear PASS; giữ user comment, pre-existing red và user recolor; khôi phục màu gốc; stress 12.000 ký tự rerun 91 ms thay vì treo trên 90 giây trước sửa. Injected-failure rollback, golden DOC/DOCX và Word 2010/x86 NOT_RUN |
| LOCAL-SCAN-RERUN-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalScanSmoke.exe` | Cả DOCX và DOC PASS chuỗi quét thể thức lần 1, sửa font, lưu, quét lần 2; lần 2 nhận thay đổi, thay annotation cũ và hoàn tất dưới 6 giây trong fixture Word COM |
| COMMENT-PRESENTATION-001 | PASS_WORD16_X64_LOCAL_SMOKE | `AnnotationPlannerTests`; `ChuanHoa.AnnotationSmoke.exe` | Comment chỉ có hai dòng Hiện tại/Yêu cầu đúng; ownership chuyển khỏi thân comment sang bookmark + document variable có chữ ký để loại nguyên nhân `Missing content`; vẫn nhận diện/xóa comment legacy có marker ẩn; apply/rerun/clear và stress 12.000 ký tự PASS |
| LEGAL-BASIS-SHORT-DATE-001 | PASS_UNIT_SCOPE | `CanonicalRouteScannerTests` | `ngày 05/03/2020` được chấp nhận ở cả Căn cứ và ngoài Căn cứ; `05/03/2020` vẫn tính là có ngày cho kiểm tra đủ thành phần viện dẫn nhưng kiểm tra chính tả tạo đúng một finding yêu cầu thêm từ `ngày`; thiếu cơ quan ban hành vẫn fail |
| ONE-CLICK-SPELLING-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.OneClickSmoke.exe` | 1-Click quét cả format và spelling; tự sửa khoảng trắng, khoảng trắng trước dấu câu, ký tự ẩn, từ điển có replacement, viết hoa đầu câu/nội dung và thêm `ngày` trước short date; chụp lại vị trí sau mutation thể thức trước khi sửa chữ; quét lại và chỉ comment/tô đỏ finding còn lại; DOC/DOCX đều sửa đủ 6 lỗi mẫu, gồm `Quyết định xố` và `ự án`, và PASS |
| VIETNAMESE-LEXICON-001 | PASS_UNIT_SIGNED_RULE_AND_WORD16_X64_DOC_DOCX | `VietnameseLexiconSpellCheckerTests`; signed-rule smoke; `ChuanHoa.OneClickSmoke.exe` | Gói quy tắc `LOCAL-DEVELOPMENT-ND30-V2` version `1.1.0` chứa 6.710 mục từ tiếng Việt; chấp nhận cả hai kiểu đặt dấu. Tên riêng/chữ viết tắt không tự sửa; từ lạ chỉ tự sửa khi có duy nhất một gợi ý chắc chắn. Cache 5.000 lần kiểm tra hoàn tất trong 2–6 ms. |
| SELECTED-FINDING-FIX-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.OneClickSmoke.exe`; `validate_vsto_source.py` | Tạo comment thật cho lỗi `Nghị định 214...`, đưa selection về caret an toàn, sửa thành `Nghị định số 214/2025/NĐ-CP`, xóa đúng comment và sau đó chạy 1-Click vẫn giữ nguyên toàn bộ viện dẫn; không tạo backup; DOC/DOCX PASS. Source gate xác nhận khi `Resolved=true` không gọi hộp thoại thành công; trường hợp không sửa được hoặc còn lỗi vẫn fail closed và hiện cảnh báo. |
| MODERN-COMMENTS-ACTIVE-DOCUMENT-001 | PASS_ROOT_CAUSE_SOURCE_BUILD_WORD16_X64_SMOKE_USER_UX_PENDING | Ảnh/video người dùng; `validate_vsto_source.py`; DOC/DOCX selected-fix -> 1-Click smoke; VSTO build/load 1.0.0.55 | Capability giữ fallback thứ hai là document đã được đánh giá ổn định và chỉ xóa khi đúng COM document đóng; Modern Comments focus thu selection về caret trước khi chọn nên không thể để stale range thay thế mất viện dẫn ở lệnh sau. Bản 1.0.0.55 đã cài, LoadBehavior=3 và tab Ribbon load trong Word 16 x64. Công cụ điều khiển cửa sổ trả nhầm ảnh ứng dụng khác nên thử nghiệm click tự động được dừng; cần người dùng xác nhận UX click trực tiếp, không nâng bước này thành PASS trực quan. |
| TABLE-MERGED-HEADER-001 | PASS_WORD16_X64_DOC_DOCX | `ChuanHoa.LocalCommandSmoke.exe` | Duyệt toàn bộ outer table; nhận diện hai dòng tiêu đề in đậm liên tiếp; bảng có gộp ngang và gộp dọc được đặt Repeat Header bằng lệnh Word native; xác nhận hai dòng header true và các dòng dữ liệu false trên DOC/DOCX |
| WINDOW-ACTIVATE-NO-SCAN-001 | PASS_SOURCE_GATE | `validate_vsto_source.py` | `WindowActivate` chỉ ghi nhận document/context và invalidate Ribbon; gate thất bại nếu handler gọi snapshot builder; đọc nội dung và nhận diện chỉ chạy theo lệnh người dùng |
| SPELLING-LIST-MARKER-001 | PASS_UNIT_BOUNDARY | `LocalDocumentScannerTests`; full `ChuanHoa.slnx` | `a.`, `a)`, `1.`, `1)`, `(1)`, gạch đầu dòng, bullet và chỉ mục lồng nhau được tách khỏi nội dung; `a. Quy mô` không cảnh báo, còn `a. quy mô` chỉ tạo một finding neo chính xác vào chữ `q`; full solution 246/246 PASS |
| BODY-LIST-TABSTOP-001 | PASS_WORD16_X64_DOC_DOCX | `ParagraphIndentPolicyTests`; `validate_vsto_source.py`; `ChuanHoa.OneClickSmoke.exe` | Đoạn nội dung thường đặt lại Left/RightIndent và xóa custom TabStops. Dòng bắt đầu bằng `-`, en/em dash hoặc bullet dùng hanging indent: marker 10 mm, nội dung và dòng xuống hàng 15 mm; stale custom tab 80 mm bị xóa, custom tab 15 mm tồn tại; DOC/DOCX PASS. |
| WORD-SNAPSHOT-001 | PASS_WORD16_X64_LOCAL_SMOKE | `ChuanHoa.SnapshotSmoke.exe "Hướng dẫn 05.docx" "Nghị định 30.doc"`; local scan smoke | DOC/DOCX snapshot PASS; đọc section/main/header/footer/footnote/endnote/text frame/table/protected span và Line Shape; SHA-256/fingerprint đổi khi shape đổi, timestamp nguồn không đổi; unsaved fail closed |
| VSTO-BUILD-001 | PASS_LOCAL_DEVELOPMENT | VS Build Tools MSBuild, Release, signed manifests bằng certificate development non-exportable | Tạo `.dll`, `.dll.manifest`, `.vsto`; 0 warning, 0 error; chưa đạt production signing |
| MODERN-COMMENTS-ONECLICK-001 | PASS_WORD16_X64_DOC_DOCX_SMOKE | `WordFindingAnnotationAdapter.TryFocusDocumentSelection`; `TryFocusDocumentStart`; `validate_vsto_source.py`; `ChuanHoa.OneClickSmoke.exe` | 1-Click ưu tiên ánh xạ Modern Comment về document scope; nếu Word đang ở trạng thái comment tạm thời không ánh xạ được thì chuyển tới range thu gọn ở đầu chính tài liệu, không retry/sleep/DoEvents và không hủy lệnh. Selected-fix rồi 1-Click DOC/DOCX PASS; thao tác click trực quan trong Modern Comments vẫn cần người dùng xác nhận. |
| LEGAL-BASIS-PUNCT-001 | PASS_UNIT_SOURCE_BUILD | `CanonicalRouteScannerTests`; full solution; signed VSTO Release rebuild | Dấu kết thúc được tính theo từng khối căn cứ liên tục: các dòng trước dùng `;`, dòng cuối dùng `.`; một khối có một căn cứ phải dùng `.`. 1-Click và Sửa lỗi đang chọn có phương án sửa xác định. |
| LIST-MARKER-LEXICON-001 | PASS_UNIT_SOURCE_BUILD | `LocalDocumentScannerTests`; full solution | `a)`, `b)`, `c)`, `a.`, `1.`, `(1)` và chỉ mục lồng nhau không bị tra như từ đơn lẻ; từ sai sau chỉ mục vẫn bị phát hiện. |
| LEGAL-BASIS-CONTEXT-001 | PASS_UNIT_SOURCE_BUILD | `DocumentRoleDetectorTests`; `CanonicalRouteScannerTests`; signed VSTO Release rebuild | Vai trò căn cứ chỉ mở sau tên loại văn bản, cho phép trích yếu/cơ quan ban hành làm cầu nối, đồng thời phải mở đầu bằng nguồn quy phạm/hành chính, thẩm quyền, nhiệm vụ hoặc đề nghị chính thức. Câu kể `Căn cứ các tài liệu được cung cấp, kết quả thẩm định...` không bị nhận nhầm ngay cả khi nằm liền sau trích yếu. |
| POINT-STYLE-AUTOFIX-001 | PASS_WORD16_X64_DOC_DOCX | `CanonicalRouteScannerTests`; `validate_vsto_source.py`; `ChuanHoa.OneClickSmoke.exe` | Phát hiện điểm đang đậm, nghiêng hoặc lẫn kiểu; comment nêu đúng trạng thái. 1-Click và Sửa lỗi đang chọn ưu tiên cấu trúc `a)`, `b)`, `c)` độc lập với vai trò suy đoán rồi đặt toàn điểm về chữ đứng, không đậm; DOC/DOCX PASS. |
| DOCUMENT-OPERATION-STATE-001 | PASS_SOURCE_BUILD_WORD16_X64_SMOKE | `DocumentOperationSession`; snapshot/annotation batch checkpoints; cancellation unit tests; four Word smokes | Có đủ Idle/Capturing/Scanning/Annotating/Mutating/Cancelling/FailedRecoverable; trạng thái và StatusBar chỉ chạy trên STA owner thread. Esc được kiểm tra ở biên batch Shape/bảng/vùng bảo vệ/paragraph/comment; khi bước qua mutation thì hủy bị tắt vĩnh viễn cho command để không tạo tài liệu nửa sửa. `finally` trả state về Idle; source gate cấm DoEvents và Task.Run. |
| VSTO-PUBLISH-001 | PASS_DEVELOPMENT_RULE_ONLY | MSBuild Publish, version 1.0.0.82; `audit_development_installer.ps1`; registry/hash/version audit; Word COM load | Một EXE 250.152 byte, SHA-256 `7209D8A9D5006724E44DB992FF21E6D693B98FBAFF6BA45E4C43FCD14DC9BEF4`, chữ ký Development hợp lệ. Payload allowlist đúng 7 file, ForbiddenHits=0 cho AI/ONNX/QR. Đã cài `Current` 1.0.0.82, Assembly/File/ProductVersion đều 1.0.0.82, LoadBehavior=3 và Word 16 x64 `Connect=True`. Không phải production signing. |
| VSTO-DEV-UPGRADE-001 | PASS_INSTALLED_WORD16_X64 | Registry/current-manifest/hash audit; `DevelopmentTestBootstrapper` Release build | Bootstrapper 1.0.0.59 cài vào `DevelopmentInstaller/Current`; registry `HKCU\Software\ChuanHoa\DevelopmentInstaller` ghi 1.0.0.59, manifest trỏ tới Current, LoadBehavior=3; hash DLL cài đặt khớp DLL Development vừa kiểm thử. Project override và source gate xác nhận build thông thường không còn ghi đè registry sang `bin\Development`; VSTO rebuild/publish và bootstrapper build đều 0 warning/error. |
| RIBBON-CACHE-001 | PARTIAL_MITIGATION_SUPERSEDED | Word đóng; registry audit; nhiều lần Word clean start; Windows UI visual/accessibility inspection | Xóa riêng value `ChuanHoa.AddIn.Vsto.Microsoft.Word.Document` trong `CustomUIValidationCache` giúp `.58` hiện tab ở lần mở ngay sau nâng cấp nhưng lỗi tái phát: video người dùng và kiểm tra trực tiếp lúc 20:43 xác nhận tab mất dù `COMAddIn.Connect=True`, `LoadBehavior=3`, manifest đúng và cache đã được Office tạo lại. Không còn coi xóa cache là sửa nguyên nhân gốc; cơ chế xóa đúng value vẫn được giữ như bước vệ sinh khi nâng cấp. |
| RIBBON-STARTUP-001 | PASS_WORD16_X64_FOUR_CONSECUTIVE_LAUNCHES | Video người dùng; COM add-in/registry/cache audit; source gate; VSTO Development rebuild/publish; bốn lần Word `/w` clean start | Nguyên nhân gốc là `CreateRibbonExtensibilityObject` gọi `EnsureRuntime()` quá sớm, dựng Word COM/access cache/timer/scanner ngay trong pha Office hỏi giao diện. Nếu pha này gặp trạng thái tạm thời, add-in vẫn có thể `Connect=True` nhưng Office không nhận Ribbon. Bản 1.0.0.59 chỉ tạo adapter nhẹ ở pha discovery, gắn runtime sau VSTO Startup và invalidate Ribbon; callback dùng adapter fail-closed khi runtime chưa sẵn sàng. Bốn lần mở liên tiếp đều hiện tab, trong đó ba lần sau `CustomUIValidationCache` đã tồn tại với value `3940032965`. |
| VSTO-INSTALL-TRUST-001 | BLOCKED_TRUST | `VSTOInstaller.exe /Install D:/ChuanHoaPublishLocal/ChuanHoa.AddIn.Vsto.vsto /Silent` | Exit `-300`: chứng thư development không được VSTO trust; không dùng bypass. Cài production cần certificate publisher và trust policy thật. |
| VSTO-WORD16-X64-001 | PASS_CURRENT_LOCAL_INSTALL_1.0.0.59_VISUAL | Word 16.0.20326.20112 x64; Windows UI visual/accessibility inspection | Add-in 1.0.0.59 đăng ký LoadBehavior=3, manifest cài đặt trực tiếp hợp lệ; bốn lần khởi động Word sạch liên tiếp đều hiển thị tab `Chuẩn hóa`, kể cả khi Office cache đã được tạo lại. Mở tab thành công và thấy các nhóm 1-Click, Khởi động, Định dạng, Bảng biểu và hình ảnh, Chính tả và số, Hiển thị, About. Nút scan bị khóa trên Document1 trống chưa lưu là capability đúng, không phải lỗi load. |
| WORD-VM-001 | NOT_RUN | Word 2010–M365 x86/x64 matrix | Chưa có VM/Office licenses và signed build |
| SIGNING-001 | BLOCKED_DECISION | Production manifest/Authenticode signing | Chưa có certificate/KMS/HSM production |

Không trạng thái BLOCKED_ENVIRONMENT, BLOCKED_DECISION hoặc NOT_RUN nào được tính thành PASS. `PASS_LOCAL_DEVELOPMENT` không thay thế production signing hoặc compatibility matrix.

`PASS_SOURCE_ONLY` chỉ xác nhận contract/source tĩnh; không thay thế build, Word load, COM integration hoặc compatibility evidence.

`PASS_FOUNDATION_INTEGRATED_FAIL_CLOSED` xác nhận các seam production đã tồn tại và không mở command khi thiếu khóa/grant. Nó không thay thế key provider/KMS thật, server replay, golden DOC/DOCX, real-Word rollback hoặc compatibility evidence.
